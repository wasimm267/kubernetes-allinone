apiVersion: v1
kind: Service
metadata:
  name: {{ include "apache-httpd.fullname" . }}
  labels:
    {{- include "apache-httpd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apache-httpd.selectorLabels" . | nindent 4 }}
