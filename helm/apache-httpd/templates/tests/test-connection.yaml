apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apache-httpd.fullname" . }}-test-connection"
  labels:
    {{- include "apache-httpd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "apache-httpd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
